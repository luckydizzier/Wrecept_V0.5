<Window x:Class="Wrecept.InvoiceEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Számla" Height="200" Width="400" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Sorszám:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding Invoice.SerialNumber}" Width="100" IsReadOnly="{Binding IsReadOnly}" />
        </StackPanel>
        <Button Grid.Row="1" Content="Bezár" HorizontalAlignment="Right" Width="80" Click="OnClose"/>
    </Grid>
</Window>
