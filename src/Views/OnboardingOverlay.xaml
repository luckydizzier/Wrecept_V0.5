<Window x:Class="Wrecept.Views.OnboardingOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#80000000"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner">
    <Border Background="White" Opacity="0.9" Padding="{DynamicResource SpacingLarge}">
        <StackPanel>
            <TextBlock Text="Gyorsbillentyűk" FontWeight="Bold" Margin="{DynamicResource MarginBottomMedium}" FontSize="{DynamicResource BaseFontSize}" />
            <TextBlock Text="Fel/Le: navigáció" FontSize="{DynamicResource BaseFontSize}" />
            <TextBlock Text="Enter: megnyitás" FontSize="{DynamicResource BaseFontSize}" />
            <TextBlock Text="Esc: vissza vagy menü" FontSize="{DynamicResource BaseFontSize}" />
            <TextBlock Text="F1: súgó" FontSize="{DynamicResource BaseFontSize}" />
            <Button Content="Bezár" Click="OnClose" HorizontalAlignment="Right" Margin="{DynamicResource MarginTopLarge}" TabIndex="0" IsDefault="True" FontSize="{DynamicResource BaseFontSize}" />
        </StackPanel>
    </Border>
</Window>
