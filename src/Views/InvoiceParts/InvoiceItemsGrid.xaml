<UserControl x:Class="Wrecept.Views.InvoiceParts.InvoiceItemsGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <DataGrid x:Name="ItemsGrid"
                  ItemsSource="{Binding Rows}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  TabIndex="0"
                  Loaded="ItemsGrid_OnLoaded"
                  KeyDown="ItemsGrid_OnKeyDown">
        <DataGrid.RowStyle>
            <Style TargetType="DataGridRow">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsPlaceholder}" Value="True">
                        <Setter Property="Background" Value="#FFEFEF" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </DataGrid.RowStyle>
        <DataGrid.Columns>
            <DataGridTextColumn Header="Termék" Binding="{Binding ProductName, UpdateSourceTrigger=PropertyChanged}" />
            <DataGridTextColumn Header="Mennyiség" Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" />
            <DataGridTextColumn Header="Egység" Binding="{Binding UnitName, UpdateSourceTrigger=PropertyChanged}" />
            <DataGridTextColumn Header="Egységár" Binding="{Binding UnitPriceNet, UpdateSourceTrigger=PropertyChanged}" />
            <DataGridTextColumn Header="ÁFA %" Binding="{Binding VatRatePercent, UpdateSourceTrigger=PropertyChanged}" />
            <DataGridTextColumn Header="Nettó" Binding="{Binding Net}" IsReadOnly="True" />
            <DataGridTextColumn Header="ÁFA" Binding="{Binding Vat}" IsReadOnly="True" />
            <DataGridTextColumn Header="Bruttó" Binding="{Binding Gross}" IsReadOnly="True" />
            <DataGridTemplateColumn Width="Auto">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Button Content="➕"
                                Command="{Binding DataContext.AddItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                TabIndex="-1" />
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
        </DataGrid.Columns>
        </DataGrid>
        <Button Content="Tétel hozzáadása"
                Command="{Binding AddItemCommand}"
                HorizontalAlignment="Left"
                Margin="0,5,0,0"
                TabIndex="1" />
    </StackPanel>
</UserControl>
