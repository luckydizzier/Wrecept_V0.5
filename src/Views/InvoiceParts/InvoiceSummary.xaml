<UserControl x:Class="Wrecept.Views.InvoiceParts.InvoiceSummary"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <DataGrid x:Name="VatGrid" Grid.Column="0" ItemsSource="{Binding VatSummaries}" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" RowHeight="{DynamicResource RowHeight}" TabIndex="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ÁFA %" Binding="{Binding Rate}" />
                <DataGridTextColumn Header="Nettó" Binding="{Binding Net}" />
                <DataGridTextColumn Header="ÁFA" Binding="{Binding Vat}" />
                <DataGridTextColumn Header="Bruttó" Binding="{Binding Gross}" />
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Column="1" Orientation="Vertical" KeyboardNavigation.TabIndex="1">
            <StackPanel.Margin>
                <Thickness Left="{DynamicResource SpacingLarge}" Top="0" Right="0" Bottom="0" />
            </StackPanel.Margin>
            <TextBlock Text="Nettó összesen:"/>
            <TextBlock Text="{Binding GrandTotals.Net}" FontSize="{DynamicResource BaseFontSize}" Foreground="Yellow"/>
            <TextBlock Text="ÁFA összesen:"/>
            <TextBlock Text="{Binding GrandTotals.Vat}" FontSize="{DynamicResource BaseFontSize}" Foreground="Yellow"/>
            <TextBlock Text="Bruttó összesen:"/>
            <TextBlock Text="{Binding GrandTotals.Gross}" FontSize="{DynamicResource BaseFontSize}" Foreground="Yellow"/>
            <TextBlock Text="{Binding GrandTotals.AmountText}" FontSize="{DynamicResource BaseFontSize}"/>
        </StackPanel>
    </Grid>
</UserControl>
