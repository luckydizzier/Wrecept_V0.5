<Window x:Class="Wrecept.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wrecept"
        xmlns:viewModels="clr-namespace:Wrecept.ViewModels"
        mc:Ignorable="d"
        Title="Főablak" Height="450" Width="800"
        DataContext="{DynamicResource MainWindowViewModel}"
        PreviewKeyDown="MainWindow_OnPreviewKeyDown">
    <Window.Resources>
        <viewModels:MainWindowViewModel x:Key="MainWindowViewModel" />
    </Window.Resources>
    <Window.InputBindings>
        <KeyBinding Key="F1" Command="{Binding ShowOnboardingOverlayCommand}" />
    </Window.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Menu x:Name="MenuBar" Grid.Row="0">
            <MenuItem Header="_Számlák" InputGestureText="Alt+S">
                <MenuItem Header="Számlák kezelése" Command="{Binding OpenInvoiceListViewCommand}" />
                <MenuItem Header="Számlák aktualizálása" Command="{Binding RefreshInvoiceDataCommand}" />
            </MenuItem>
            <MenuItem Header="_Törzsek" InputGestureText="Alt+T">
                <MenuItem Header="Szállítók" Command="{Binding OpenSupplierViewCommand}" />
                <MenuItem Header="Termékek" Command="{Binding OpenProductViewCommand}" />
            </MenuItem>
            <MenuItem Header="_Listák" InputGestureText="Alt+L">
                <MenuItem Header="Dátum alapú keresés" Command="{Binding FilterByDateViewCommand}" />
                <MenuItem Header="Szállító alapú keresés" Command="{Binding FilterBySupplierViewCommand}" />
                <MenuItem Header="Termékcsoport alapú keresés" Command="{Binding FilterByProductGroupViewCommand}" />
                <MenuItem Header="Termék alapú keresés" Command="{Binding FilterByProductViewCommand}" />
            </MenuItem>
            <MenuItem Header="_Súgó" InputGestureText="Alt+H">
                <MenuItem Header="Billentyűk" Command="{Binding ShowOnboardingOverlayCommand}" InputGestureText="F1" />
                <MenuItem Header="Súgó" Command="{Binding OpenHelpViewCommand}" />
                <MenuItem Header="Névjegy" Command="{Binding OpenAboutDialogCommand}" />
                <MenuItem Header="Beállítások" Command="{Binding OpenSettingsViewCommand}" />
                <Separator />
                <MenuItem Header="Kilépés" Command="{Binding ExitApplicationCommand}" />
            </MenuItem>
        </Menu>
        <TextBlock Grid.Row="1" Text="{Binding Greeting}" Margin="10" />
        <DataGrid x:Name="InvoiceGrid"
                  Grid.Row="2"
                  ItemsSource="{Binding Invoices}"
                  SelectedItem="{Binding SelectedInvoice, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  TabIndex="0"
                  Margin="10"
                  PreviewKeyDown="InvoiceGrid_OnPreviewKeyDown">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Sorszám" Binding="{Binding SerialNumber}" />
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Row="3" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Új számla" Command="{Binding AddInvoiceCommand}" Margin="0,0,5,0" />
                <Button Content="Törlés" Command="{Binding DeleteInvoiceCommand}" CommandParameter="{Binding SelectedInvoice}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
